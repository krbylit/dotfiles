> [!WARNING] CRITICAL: Do not edit files in `~/.config`
> These dotfiles are managed by Chezmoi.
> Any changes to files under `~/.config` will not persist, they will be overwritten the next time `chezmoi apply` runs.
> All editing of config files must be done in `~/.local/share/chezmoi`.
