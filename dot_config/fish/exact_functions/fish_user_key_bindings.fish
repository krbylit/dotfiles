function fish_user_key_bindings
    # Fish runs this last whenever it sets up keys, so we can have final say in overriding anything here.
    # This can run often though, so may want to be sparing.
end
